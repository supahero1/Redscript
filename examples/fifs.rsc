use lang;
x = 68;

// do math => if math not 0 => store yes in cmp0
if (x != 0)
{
// if cmp0 do stuff
// if cmp0 do more stuff
}

//      cmp0   and  cmp1     = cmp2
if (x % 2 == 0 and x == 2)
{
    msg(@r, "X is even and x is 2!");
}
else // not cmp2
{

}